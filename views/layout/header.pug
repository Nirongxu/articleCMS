header.main-header.pt40
    .container
        .row
            .col-lg-12
                p.topTitle
                    a 标题
nav.main-navigation
    .container
        .row
            .col-lg-10
                ul
                    li
                        a(href="#") 导航
                    li
                        a(href="#") 导航
                    li
                        a(href="#") 导航
            .col-lg-2
                p.login
                    if session && !session.isNew
                        span.name= session.username
                        a(href="/user/logout") 退出

                    else
                        a(href="/login") 注册
                        a(href="/login") 登录
